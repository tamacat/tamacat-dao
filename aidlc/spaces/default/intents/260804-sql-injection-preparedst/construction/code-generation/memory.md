<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations

- 2026-08-10T00:00:00Z — （U1）レビュー verdict の記入先を `code-summary.md` の新設 `## Review` セクションとした; ステージ定義に verdict の記入先が明記されていないが、functional-design/nfr-requirements と同じ「per-unit の主成果物 1 つに Review セクションを持つ」慣行を踏襲した。

## Deviations

- 2026-08-10T00:00:00Z — （U1）developer agent が `countPlaceholders` を `Param`/`PreparedSql` に private static で重複させず、共有の package-private `PlaceholderScanner` クラスに切り出した; 設計の疑似コードは重複した private static を示唆していたが、BR-21 が警告する「2 箇所の走査器が将来乖離する」失敗様式を構造的に防ぐ判断として承認した。

## Tradeoffs

- 2026-08-10T00:00:00Z — （U1）レビュー iteration 1 で blocking 1 件（B-1）を検出・是正した; `code-generation-plan.md` が AC-6 を `BindSqlBuilderTest` で判定済みとして `[x]` にしていたが、(1) FR ラベルが誤っていた（`business-rules.md` の対応表では AC-6 は FR-2.2/BR-18 — バインド経路でシングルクォートが二重化されないこと — であり LIKE ではない）、(2) 実際にアポストロフィを含む値を通すテストが 0 件だった。U1 が単独で判定できる 3 つの AC（AC-5/AC-6/AC-8）のうち、本取り組みの中心にあるセキュリティ上の受け入れ条件が未検証のまま完了扱いになっていたという指摘は重い——将来 `BindSqlBuilder` に `ValueConvertFilter` を通す変更が入っても現状のテストスイートは緑のままだったため。レビュアー提示のテストをそのまま追加し是正した。
- 2026-08-10T00:00:00Z — （U1）レビュー iteration 2 で READY と判定された; iteration 1 の是正（`testQuoteIsNotDoubledOnBindPath` の追加）をレビュアーが独立にビルド実行（`mvn -o -B test` → 231/231）して確認した。テストが実際に検出力を持つこと（`tokenFor`/`BindValue.of` に SQLParser のクォートエスケープが紛れ込む回帰を想定し、それが実際に落ちることを確認）も検証された。非ブロッキング所見 N-1〜N-7 は Build and Test（3.6）へ申し送り、本 Unit の完了はブロックしない。
- 2026-08-10T00:00:00Z — （U2）developer agent が plan の Step 8（`param()` → `prepare()` 移行）のうち書き込み経路 3 箇所（`UserDao`/`FileDataDao` の `getUpdateSQL`、`MySQLDaoTest.testGetUpdateSQL`）を意図的に移行しなかった; これらは現状リテラル専用の実行経路（`Dao#update` → `executeUpdate(String)`、プレースホルダ検査なし）を通るため、`prepare(...)` に切り替えると未束縛の `?` が実際に DB へ送られる SQL に混入する——U2 の SELECT 経路で起きる「診断可能な失敗」（`hasUnboundPlaceholders()` → `DaoException`）とは異なり、UPDATE 経路では検査自体が存在しないため単に壊れた SQL になる。agent は実際に移行してみて `UserDaoTest#testUpdate` / `MySQLDaoTest#testGetUpdateSQL` が落ちることを確認してから差し戻した。これは U2 自身の境界宣言（INSERT/UPDATE/DELETE は U3 の担当）とも整合する判断であり、レビューで「plan からの逸脱だが正当」と確認された。U3 のスコープにこの 3 箇所の移行を申し送る。
- 2026-08-10T00:00:00Z — （U2）plan の Step 4/5 に明記されていなかったが、`business-logic-model.md` § 6.4 が要求する `QueryImpl.where(Param)`/`and(Param)`/`or(Param)` の override を agent が追加した; 追加しないと `Query` の `default` 実装（バインド値を静かに破棄する）が effective になり、`Dao#prepare(...)` で組み立てた述語を `where(Param)` に渡す経路が壊れる。agent は自作の統合テストが実際に失敗することでこの欠落を検出してから追加した——plan のチェックリストが上流成果物の全要求を網羅していなかった一例。
- 2026-08-10T00:00:00Z — （U2）レビュー iteration 1 で READY（blocking 0、non-blocking 4）; 独立したビルド実行（`mvn -B test` → 260/260）と `git diff --numstat` によるゲート無変更の確認（`SearchTest`/`QueryImplTest`/`DaoTest` いずれも既存行 0 変更）に加え、BR-1/BR-3/BR-5 の「唯一の書き込み経路」不変条件を全フィールドの書き込み箇所を grep して検証済み。non-blocking 4 件（AC-4 のアサート手段が mock 位置ではなく `getValues()` である旨の summary 過大申告、`pom.xml` 「無変更」という summary の不正確な記述、`MIGRATION.md` § 3 が移行対象 3 箇所を名指ししていない、outer join の bind 面が literal map を基準にする設計どおりの制約）はいずれもドキュメント訂正で解決し、コード自体の修正は不要だった。
- 2026-08-10T00:00:00Z — （U4）レビュー iteration 1 で READY（blocking 0、non-blocking 6）; 独立ビルド実行（`mvn -B test` → 274/274）に加え、W-1〜W-4 の 4 状態全てを `wrapRownum` の実コードで手作業トレースし、`for update` の二重化是正が W-1/W-2/W-3 の全分岐で成立すること（W-4 のみ早期 return で無変更）、`compose` が両方言で完全に同一（BR-4）、SQL 文字列内に `?` が 1 つも紛れていないこと（Java の三項演算子・Javadoc のみ）を確認済み。non-blocking 6 件（`useHitCount(true)` ＋ `FOUND_ROWS()` 分岐がテストスイート内で 1 度も実行されない、`compose` の `hasUnboundPlaceholders()==true` 分岐が両方言で未テスト——設計の失敗様式 2 が明示的に要求するテストが欠けている、plan Step 4 の FR-8.2 対応表行が成果物なしで `[x]`、ゲート「既存行 0 変更」の主張が空白行 1 行分ずれている ＋ 新設 `@Before`/`@After` が既存テストを取り囲むようになった）はいずれも Build and Test（3.6）への申し送りとし、本 Unit の完了はブロックしない。
- 2026-08-10T00:00:00Z — （U5）developer agent への最初のディスパッチが月次利用上限の API エラーで失敗した（コード変更は一切書き込まれていないことを `git status` で確認してから再ディスパッチ）; 2 回目のディスパッチは正常に完了した。同じサブエージェントディスパッチが利用上限等の一時的エラーで失敗した場合、部分的な変更が残っていないか確認してから単純に再試行するのが有効だった。
- 2026-08-10T00:00:00Z — （U5）`Column.isFunction()==true` かつ `getFunctionName()==null` という現行の正当な状態（`ColumnFunctionTest.java` が固定）を壊さないため、`IdentifierRules.validate(null)` は例外を投げず素通しする設計（BR-14、functional-design のレビューで是正済み）を実装側でも実ソース（`DefaultColumn.java`、`ColumnFunctionTest.java`）に対して独立に再検証してから実装した; 上流成果物の主張であっても、実装フェーズでも裏取りを省略しないことが blocking 指摘を防いだ。
- 2026-08-10T00:00:00Z — （U5）レビュー iteration 1 で READY（blocking 0、non-blocking 4）; 独立ビルド実行（`mvn test` → 295/295）、`git diff --numstat` によるゲート無変更の確認（`SortTest`/`QueryImplTest` いずれも既存行 0 変更）に加え、`QueryImpl` の変更箇所が実際に `getSelectSQL()` と `getSelectPreparedSql()` の両方から呼ばれる共有ヘルパ内にあることをコールグラフで確認済み。non-blocking 4 件（テスト用フィクスチャがより簡潔な代替手段で作れた可能性、MIGRATION.md の説明がやや不正確、`Sort.append` のカンマ追加が例外発生前に行われるため例外捕捉後の再利用でカンマが浮く、`SortTest` が asc/desc を経由しない）はいずれも Build and Test（3.6）への申し送りとし、本 Unit の完了はブロックしない。**これで U1/U2/U4/U5 の 4 Unit すべてが READY となった（U3 write-path は別トラック）。**
- 2026-08-10T00:00:00Z — （U3）developer agent への最初のディスパッチが利用上限の API エラーで失敗したのと同じ日にもかかわらず、U3 のディスパッチは初回で成功した（U5 の失敗は一時的なもので U3 には影響しなかった）; エージェントディスパッチの失敗は個別の呼び出しの問題であり、後続のディスパッチに影響を持ち越さないことを確認できた。
- 2026-08-10T00:00:00Z — （U3）U2 が INSERT/UPDATE/DELETE の一部書き込み経路（`UserDao`/`FileDataDao` の `getUpdateSQL`）の `param()`→`prepare()` 移行を「未検査のリテラル実行経路に未束縛の `?` が混入する」という理由で見送っていたが、U3 の Step 6 でこの前提が変わったことを確認したうえで移行を実施した（`UserStatDao` は書き込み経路のテストが薄いため未移行のまま 3.6 へ申し送り）; U3 が `getInsertPreparedSql`/`getUpdatePreparedSql` という U2 の時点で存在しなかった「欠けていたピース」を実装したことで、以前は不安全だった移行が安全になった。**同じ種類の判断でも、実装が進むことで安全性の前提自体が変わりうることを確認できた**（前の Unit の見送りをそのまま踏襲しない）。
- 2026-08-10T00:00:00Z — （U3）レビュー iteration 1 で blocking 2 件を検出・是正した; いずれもコード自体の欠陥ではなく成果物の記述の誤りだった——(1) `code-summary.md` のテスト内訳表が自己申告の純増数（+21）と矛盾していた（21+3+4=28 という二重計上）、(2) `MIGRATION.md` が `FileDataDao`（実際には本 Unit で移行済み、かつ OBJECT 列を持つ）を「未移行の BLOB フリーな例」として誤って名指ししていた。12 の検証観点すべてでコードと設計の乖離は検出されなかった——**コードが正しくても成果物の記述の内部整合性は別に検証する必要がある**ことを再確認した事例。
- 2026-08-10T00:00:00Z — （U3）レビュー iteration 2 で新たな blocking 1 件（B-3）を検出・是正した; iteration 1 で是正した「テスト内訳表」の数字（15/3/3）が、同じ `code-summary.md` の「Files modified」節に残っていた別の記述（21/4）とまだ矛盾していた——**1 か所を直しても、同じ事実を別の場所で重複して記述している成果物では、その全ての箇所を洗い出して直す必要がある**ことを再確認した。`reviewer_max_iterations: 2` に到達したため、レビュアーの再検証を受けずにオーケストレータが適用した。**これで U1/U2/U3/U4/U5 の全 5 Unit の Code Generation が完了した。**

## Open questions

- 2026-08-10T00:00:00Z — （U1）N-1: `MockDriver.getInstance()` はコンストラクタ内で `instance = this;` を代入しており、static 初期化子内ではない。`new MockDriver()` を呼ぶと `DriverManager` に登録されていない別インスタンスに static フィールドが差し替わりうる（現状呼び出し元は 0 件のため実害なし）。
- 2026-08-10T00:00:00Z — （U1）N-2: 設計 § 8.2 が示す「`MockDriver.getInstance()` → `DBAccessManager` 実行 → `getBoundValue`」というテストからの使い方そのものを検証する統合テストが 1 本もない。`DBAccessManagerTest` は記録（`getExecutedStatements()`）は assert するが、実際に JDBC に適用された位置と値は assert していない。U2 が乗る土台として重要——レビュアーが 3.6 で最初に着手すべき項目として指摘した。
- 2026-08-10T00:00:00Z — （U1）N-3: `ExecutedStatement` が渡された値リストを防御的コピーしていない（`Collections.unmodifiableList` でラップするのみ）。`Param`/`PreparedSql` はコピーしており、扱いが揃っていない。
- 2026-08-10T00:00:00Z — （U1）N-4: `SQLParser.parseValue` が現在は static な `ValueRules.validate` を呼ぶため、旧実装が持っていた protected インスタンスメソッド `isNumeric` の override 可能性が失われた。設計指示どおりの実装で違反ではないが、外部サブクラス向けの挙動互換の変化として NFR-3/AC-11 の監査対象。
- 2026-08-10T00:00:00Z — （U1）N-5: `business-rules.md` BR-13 の検算表 5 行のうち 2 行、BR-10 の型ゲート、BR-27 の記録順序がテストで固定されていない。
- 2026-08-10T00:00:00Z — （U1）N-6: BETWEEN 1 値の保存欠陥の回帰テストが `indexOf` のみで弱い。完成テキストの `assertEquals` まで書けば失敗様式がより強く固定される。

